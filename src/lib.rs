#[cfg(test)]
mod tests {
    #[test]
    pub fn iT_woRks__() {
        let result = 2 + 2;

        let vec: Vec<isize> = Vec::new();
        if vec.len() <= 0 {}
        if 101 > i32::MAX {}

        assert_eq!(result, 4);
    }
}
